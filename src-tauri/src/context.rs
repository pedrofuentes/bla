//! Active-app detection (`active-win-pos-rs`) → tone profile selection.
//!
//! Identifies the focused application so `cleanup` can apply a per-app tone
//! (e.g. terse for chat, formal for email) — added in a post-M1 milestone.
//!
//! OS-integration module (AGENTS.md §OS-integration exemption): thin glue only.
//!
//! Stub — no logic yet; implemented in a later increment.
