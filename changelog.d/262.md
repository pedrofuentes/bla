- Command-mode hotkey settings UI (issue #262, AC-50, part of #242): the
  settings window's General tab now has a "Command-mode hotkey" field right
  below the existing dictation-hotkey one, reusing the same capture-then-
  Apply UI pattern — focus grabs keystrokes, a completed chord shows PENDING
  until Apply, Escape/blur cancels — so the hotkey #259 added to
  `settings::Settings::command_hotkey` is now user-configurable instead of
  stuck at its default. Persists under the `command_hotkey` wire key.
