- Test/chore batch closing seven Sentinel test-quality follow-ups plus a format-drift fix (issues #209, #224, #225, #226, #234, #238, #240, part of #217): added negative "not committed before blur" assertions to GeneralTab's file-mode fields and HistoryTab's retention control (#209, #224); added a HistoryTab test exercising the `searchSeqRef` out-of-order-search guard and a `commitRetention` rejection/revert-path test (#225, #226); added DictionaryTab controlled-promise tests for the add/remove in-flight button states (#234); added a ToneTab test exercising the per-row `editGenRef` guard against a stale REJECT clobbering a newer, already-applied edit (#238); replaced the `tests/visual/settings-harness.tsx` Tone-tab fixture's non-synthetic `*Terminal` app pattern with a synthetic `*SynthTerm` and recaptured the affected `docs/design/screenshots/settings-tone-*.png` screenshots (#240); and ran Prettier on `src/lib/pathTemplate.ts` to fix the format drift `pnpm format:check` was flagging (#217 part 1 — part 2, adding `format:check` to the CI workflow, is a human-gated workflow change left for #217 to stay open). No production behavior changed — test-only, fixture-only, and formatting-only.
