# Sentinel Review Report — PR #127

Status: REJECTED
Ref: `feat/m2-windows-scaffold` → main (PR #127)
Report ID: SNTL-20260709-bla-PR127-dfbf164
Reviewed SHA: `dfbf164ca4e9c4eed9ae8014224db1b070979adb`
Sentinel ruleset: v1 · Severity rubric v1
Reviewed at: 2026-07-10T03:12:17Z
Mode: standard · Review depth: Tier 2 (full) · Required action: FIX_AND_REINVOKE

## Phase 1 — TDD / Test Evidence
- Tests exist & meaningful: ✅ — `pill_visibility_for_hides_on_idle_and_shows_otherwise_issue_126` (src-tauri/src/tray.rs:108); mutation probe confirmed a fully discriminating oracle.
- Test-first history verified: ✅ — RED reproduced at 558cfbb (E0425 ×4); GREEN at 99c8798.
- Full suite green on SHA: ✅ — Sentinel's own scratch-worktree runs: fmt/clippy clean; 170 unit + 4 acceptance + 1 doctest; Vitest 15/15; pnpm build emits all three pages; eslint clean.
- Coverage: N/A (template braces in docs/SENTINEL.md); changed pure logic 100% covered; lib.rs glue coverage-excluded per Makefile policy.

## Findings
🔴 CRITICAL: 2 · 🟡 IMPORTANT: 3 · 🟢 MINOR: 4

1) [🔴] Pill `window.show()` steals foreground focus on Windows from the second dictation onward, misdirecting the synthetic paste — src-tauri/src/lib.rs:490,498-504. `"focus": false` maps to one-shot MARKER_DONT_FOCUS (tao 0.35.3 windows/window_state.rs:329-333, windows/window.rs:1147); later shows use SW_SHOW and activate the pill; paste path targets the OS-focused window → silent transcript loss on Windows. Fix: add `"focusable": false` to the pill window (WindowConfig.focusable, tauri-utils 2 config.rs:2019 → WS_EX_NOACTIVATE, tao windows/window_state.rs:297).

2) [🔴] `settings` window destroyed by titlebar close; tray "Settings…" silently no-ops until restart — src-tauri/src/lib.rs:529-531 with lib.rs:1014-1029 and tauri.conf.json:33-40. CloseRequested prevent_close+hide handler registered only for "main". Fix: mirror the handler for `settings` (or rebuild via WebviewWindowBuilder::from_config on None lookup).

3) [🟡] Pill visibility can desync from pipeline state under overlapping transitions (enqueue-order race) — src-tauri/src/lib.rs:474,490-504. State-write, visibility snapshot, and run_on_main_thread enqueue are not atomic; closures can apply out of order → always-on-top window stuck visible while Idle. Escalated relative of #96. Fix: re-derive visibility inside the main-thread closure from AppState::pipeline_state, or per-call sequence numbers.

4) [🟡] `transparent: true` inert on macOS without `macos-private-api` — tauri.conf.json:27; Cargo.toml lacks the feature; tauri-runtime-wry warns (lib.rs:889-891); wry transparency behind cfg(feature) (wry 0.55.1 wkwebview/mod.rs:367-382). Masked today by the placeholder's near-opaque backdrop; MUST land before the real pill UI (M2 waveform PR). Fix: `"app": { "macOSPrivateApi": true }` + `macos-private-api` tauri feature; note in DECISIONS/LEARNINGS.

5) [🟡] Destroyed pill/settings handles fail silently forever (no log, no recreation) — src-tauri/src/lib.rs:498-504. eprintln! on None lookup per file convention; consider recreate-on-demand. Largely subsumed for settings by 🔴 #2 fix.

6) [🟢] Batch: (a) csp:null now covers 3 windows — inert today (capabilities/default.json scopes ["main"] only; IPC surface NOT widened); set explicit CSP before dynamic/transcript content renders in these windows. (b) Two extra webviews created eagerly at launch — measure RSS/startup at AC-7; consider lazy creation. (c) Pill/Settings placeholders have no Vitest coverage — within scaffolding exemption; mandatory once props/IPC wire up. (d) Doc polish: changelog fragment overclaims tray shows "them" (only settings); settings/index.tsx comment says "title-bar-free" but window is decorated.

Checks with no findings: privacy invariants (no network/telemetry/clipboard logging), dependency surface (no manifest changes), AC-6 competitor audit clean, Conventional Commits, changelog fragment present, no scope creep. IPC/capability surface not widened.

## Decision rationale
Two independently verified correctness gaps under normal usage; correctness gaps are 🔴 → REJECTED, never CONDITIONAL. Exact fixes are one config line + one copied handler pattern. Everything else strong (verified RED→GREEN TDD, suites green on reviewed SHA under Sentinel's own execution).

Anchors: 🟡#3 lib.rs:474-504 @ dfbf164 · 🟡#4 tauri.conf.json:27 @ dfbf164 · 🟡#5 lib.rs:498-504 @ dfbf164
